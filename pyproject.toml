[build-system]
requires = ["setuptools>=64", "numpy>=1.22"]
build-backend = "setuptools.build_meta"

[project]
name = "pykarambola"
version = "0.5.0"
description = "Python implementation of Karambola – Minkowski tensor morphometry of 3D structures"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.9"
keywords = [
    "minkowski", "tensors", "bioimage", "morphometry",
    "3d-shape-analysis", "mesh", "geometry", "surface-analysis",
    "computational-geometry", "shape-descriptor", "materials-science",
    "minkowski-functionals", "triangulated-surface", "image-analysis",
]
classifiers = [
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy>=1.22",
    "scipy>=1.8",
]

[project.urls]
Homepage = "https://github.com/Ishihara-SynthMorph/pykarambola"
Documentation = "https://github.com/Ishihara-SynthMorph/pykarambola#readme"
"Bug Tracker" = "https://github.com/Ishihara-SynthMorph/pykarambola/issues"

[project.optional-dependencies]
dev = ["pytest", "scikit-image>=0.19"]
accel = ["cython>=3.0"]
glb = ["trimesh"]
stl = ["numpy-stl"]
notebooks = ["scikit-image>=0.19", "matplotlib", "seaborn", "pandas", "medmnist", "tifffile", "scikit-learn", "pooch"]

[tool.setuptools.packages.find]
include = ["pykarambola*"]
